import { logo as types  } from '../actionTypes'

export const logoSpinSpeedIncrement = () => ({ type: types.SPEED_INCREMENT })
export const logoSpinSpeedDecrement = () => ({ type: types.SPEED_DECREMENT })